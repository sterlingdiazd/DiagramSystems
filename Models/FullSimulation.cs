using NeedOfVigilante.Models.ExpressionTypes;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace NeedOfVigilante.Models
{
    public class FullSimulation
    {

        private DbContextDataContext dbContext = new DbContextDataContext();
        /*
        //Metodo recibe un diagrama con sus elementos y salidas a evaluar.
       public List<int> createDiagram()
       {
           List<Element> elements = createElements();
           ElementEvaluation elementOperator = new ElementEvaluation(elements);
           List<int> nextItems = elementOperator.RunElement();
           return nextItems;
       }
        */
    }
}